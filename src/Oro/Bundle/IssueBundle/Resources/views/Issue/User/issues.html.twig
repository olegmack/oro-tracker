{% if resource_granted('oro_issue_view') %}
    {% import 'OroDataGridBundle::macros.html.twig' as dataGrid %}

    <div class="responsive-cell">
        <div class="box-type1">
            <div class="title">
                <span class="widget-title">{{ "oro.issue.entity_plural_label"|trans }}</span>
            </div>

            <div class="row-fluid">
                {{ dataGrid.renderGrid('user-issues-grid', {id: entity.id}) }}
            </div>
        </div>
    </div>
{% endif %}
